// use embedded_svc::anyerror::*;
// use esp_idf_hal::prelude::*;
// use esp_idf_svc::sysloop::*;
use std::{thread, time::Duration};
use doorsi::*;

fn main() {
    println!("Hello, world!");

    
}
